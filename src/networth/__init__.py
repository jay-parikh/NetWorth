"""NetWorth — local-only family portfolio tracker.

The workbook is a build artifact: `generate` builds it entirely from code,
`reader` reads user inputs back out, and the updater (R3) round-trips the two.
See docs/SPEC.md — this package is the reference implementation of that spec.
"""

__version__ = "1.4.2"
